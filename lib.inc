;---------------------------------------------------------
;		lib.inc copyright the blueheart 2018.11.4
;---------------------------------------------------------
;						库文件准则：
;1. 凡是不涉及输出的寄存器均保持为原数据
;---------------------------------------------------------


IFNDEF LIB_INC
LIB_INC equ <1>

;---------------------------------------------------------
;子程序：比较
;功能：比较指定数据类型的两个无符号数的大小
;输入参数：ds:[si],es:[di]分别指向两个无符号数
;		   的首地址，dl存储的为数据类型大小
;输出参数：dh=1,前者大于后者
;		   dh=0,两者相等
;		   dh=-1,后者大于前者
extrn compare:far
;---------------------------------------------------------
;子程序：交换
;功能：交换一对指定类型大小的数据的空间位置
;输入参数:ds:[si],es:[di]分别指向两个无符号数的首地址，
;		   dl存储的为数据类型大小
;输出参数：无
extrn swap:far
;---------------------------------------------------------
;子程序：快排
;功能：对一组无符号数据进行排序
;输入参数:ds:[si]指向原数组，cx储存的为数组长度，
;		   dl存储的为数据类型大小
;输出参数：无
extrn qsort:far
;---------------------------------------------------------

ENDIF